# ──────────────────────────────────────────────────────────────
# Variables de entorno — Radio Sisid Ecuador
# ──────────────────────────────────────────────────────────────
# Copia este archivo a .env.local para desarrollo local.
# Las variables con prefijo VITE_ se exponen al cliente.
# Las variables sin prefijo VITE_ solo están disponibles en Node (backend).
#
# ► NUNCA subas .env.local al repositorio (ya está en .gitignore).
# ► En Vercel, configura estas variables en:
#   Settings → Environment Variables
# ──────────────────────────────────────────────────────────────

# ── Backend API (futuro) ───────────────────────────────────────
# URL base para tu API backend cuando lo implementes.
# Ejemplo: https://api.radiosisidecuador.com
# VITE_API_URL=

# ── Base de datos (futuro, solo backend / serverless) ──────────
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# ── Supabase (alternativa, futuro) ─────────────────────────────
# VITE_SUPABASE_URL=https://xxx.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGciOi...

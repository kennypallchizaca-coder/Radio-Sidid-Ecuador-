/**
 * services/index.ts
 * ──────────────────────────────────────────────────────────────
 * Barrel export de servicios.
 *
 * Cuando se agregue un backend, los servicios de API irán aquí:
 *   export { apiClient } from "./api";
 *   export { authService } from "./auth";
 */
export { resolveEcuadorRadioStream } from "./radioBrowser";
